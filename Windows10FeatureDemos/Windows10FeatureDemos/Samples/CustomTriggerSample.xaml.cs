using Windows.UI.Xaml.Controls;
using Windows10FeatureDemos.Helper;

namespace Windows10FeatureDemos.Samples
{
    [Menu(Caption = "Custom Trigger", Symbol = Symbol.Caption)]
    public sealed partial class CustomTriggerSample : UserControl
    {
        public CustomTriggerSample()
        {
            this.InitializeComponent();
        }
    }
}
