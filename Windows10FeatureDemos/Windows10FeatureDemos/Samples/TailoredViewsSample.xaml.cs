using Windows.UI.Xaml.Controls;
using Windows10FeatureDemos.Helper;

namespace Windows10FeatureDemos.Samples
{
    [Menu(Caption = "Tailored Views", Symbol = Symbol.Caption)]
    public sealed partial class TailoredViewsSample : UserControl
    {
        public TailoredViewsSample()
        {
            this.InitializeComponent();
        }
    }
}
